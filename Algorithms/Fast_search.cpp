#include<iostream>
#include<vector>
#include<algorithm>

using namespace std;

void    solve(vector<long>& vect, long x, long y, long n)
{
    
}


int main (void)
{
    long n,k;

    cin >> n;
    vector<long> tab(n);
    for (int i=0; i < n ; i++)
    {
        cin >> tab[i];
    }
    cin >> k;
    while (k--)
    {
        long x,y;
        cin >> x >> y;
    }
}