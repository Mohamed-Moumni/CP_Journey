#include <iostream>
#include <vector>
#include <map>

using namespace std;

int checkIt(int n, int m)


int main(void)
{
    int n,m;

    cin >> n >> m;
    cout << checkIt(n, m) << endl;
    return (0);
}